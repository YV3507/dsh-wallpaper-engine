# 吉祥物（拉绳）源资产

本目录归档**「聊天吉祥物」/ 拉绳功能**的原始美术素材。它们是**源资产**，不是运行时载荷：

- 运行时用的是 [`src/client.js`](../../src/client.js) 里**内联的 base64 派生版本**（单文件客户端
  自包含，见 `ROPE_IMG` / `ROPE_IMG_WHALE` 两处常量及其注释）。
- 因此本目录**不进 npm 包**（不在 `package.json` 的 `files` 白名单内），只随仓库源码分发。
- 原本散落在**仓库根目录**（三个 `ChatGPT Image …png` + 一个 hash 名 `.svg`），P0-2 归入此处并
  改成可读名。`git mv` 完成，历史可追。

## 文件与用途

| 文件 | 尺寸 | 用途 / 判定依据 |
|---|---|---|
| `rope-maid-source-1192x1320.png` | 1192×1320 | **形态 `maid`（小女仆）源图**。`ROPE_IMG` 注释写"缩小到 256×283"，而 1192∶1320 与 256∶283 长宽比一致（0.903 vs 0.905） |
| `rope-whale-source-1024x1536.png` | 1024×1536 | **形态 `whale`（鲸御姐）源图**。`ROPE_IMG_WHALE` 注释明确写 *source 1024×1536*，缩小到 384×576（2:3 精确对应） |
| `alternate-portrait-937x1679.png` | 937×1679 | **未被代码引用**的另一版竖幅立绘（疑为早期迭代或候选稿）。保留待作者认领 |
| `vector-960x1024.svg` | 960×1024（14 条 path，纯矢量） | **未被代码引用**的矢量素材（无内嵌位图、无文字）。保留待作者认领 |

> 说明：两张 PNG 的形态归属是**据源码注释 + 长宽比推得**的，不是肉眼比对（本仓不含图像识别）。
> 若作者能确认，请把上表两行"判定依据"改成直接结论；`alternate-*` 与 `vector-*` 若确认无用，
> 再单独决定去留 —— **不要因为"没被引用"就删掉源资产**。

## 替换素材时怎么重新生成内联派生版本

1. 把新立绘处理成**透明背景 PNG**。
2. 按形态缩放到既有目标尺寸（`object-fit: contain` 依赖它与渲染盒同比例）：
   - `maid` → **256×283**，写入 `ROPE_IMG`
   - `whale` → **384×576**，写入 `ROPE_IMG_WHALE`
3. 转 base64 data URI 替换对应常量，然后 `npm run build` 重建 `lib/client.js`
   （两者必须一起提交，CI 会断言产物与源码同步）。
4. 新增形态时同步 `ROPE_FORMS`（`img` + 显示名 + 基准盒 `w/h`）与 `ROPE_FORM_VALUES`，
   并遵守既有不变量：常量必须在 `readPersisted()` 之前声明（`const` TDZ）。

## 相关但不同的一处

[`docs/images/mascot-drawer.png`](../../docs/images/mascot-drawer.png) 是 README 里**功能截图**，
与本目录的**美术源文件**是两回事；截图受 `.gitignore` 白名单管理。
